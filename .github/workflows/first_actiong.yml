name: hello workflow
on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: hello world
        run: echo "Hello World"
      - name: goodbye world
        run: echo "Goodbye World"